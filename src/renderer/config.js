module.exports = {
  // number of showing items of selectpicker
  selectSize: 6,
  // distance options for infinite-loading component
  infiniteLoadingDistance: Math.round((window.innerHeight / 4) * 3),
};
