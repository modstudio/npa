export default `CREATE TABLE contacts (
  id INTEGER PRIMARY KEY,
  company_name VARCHAR(255),
  first_name VARCHAR(255),
  last_name VARCHAR(255),
  phone_number VARCHAR(25),
  address VARCHAR(255),
  city VARCHAR(255),
  state VARCHAR(255),
  zip VARCHAR(25),
  country VARCHAR(255)
);`;
